version: "3.8"

services:
  envoy:
    build:
      context: .
      dockerfile: Dockerfile.envoy
    ports:
      - "8080:8080" # gRPC-Web (frontend → Envoy)
      - "9901:9901" # Envoy admin
    networks:
      - grpc-network
    extra_hosts:
      - "host.docker.internal:host-gateway"

networks:
  grpc-network:
    driver: bridge
